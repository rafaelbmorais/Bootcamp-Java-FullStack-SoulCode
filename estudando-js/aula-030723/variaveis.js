 // Java é uma Linguagem de Programação Compilada.
 // JavaScript é uma LInguagem de Programação Interpretada.

 // Isso é um comentário de uma única linha!

 /*
    Isso é um comentário de várias linhas!
 */

// Chrome -> o interpretador do JS no navegador se chama V8.
// Node.js -> o do Node.js também é o V8 + NPM (node Package Manager)

// COMANDOS JavaScript:

// console.log("texto") // imprime no terminal/console uma informação
// console.log(5)

// VARIÁVEIS -> espaço reservado na memória ram para armazenar um valor durante a execução do programa.

let nome; // declaração da variável chamada nome
nome = "Bootcamp Java"; // atribuição do valor a varíavel nome
console.log(nome); // exibe a informação na saída

let idade; // declara outra variável chamada idade
idade = 23; // atribui um valor a variável idade
console.log(idade);

let nome2 = "Rafael"; // declaração da variável e atribuição de um valor no mesmo comando.

let altura = 1.75;
console.log(altura);

let cpf = "123.456.789-0";
console.log(cpf);

let eCasado = true; // valor booleano (true ou false)
console.log(eCasado);

idade = 38;
console.log(idade);

const PI = 3.14; // declaração da constante PI (por convenção usa-se os nomes das constantes em letras maiúsculas!).
console.log(PI); // a constante não pode ser alterada, se tentar alterar irá causar erro.

// Tipagem ESTÁTICA -> Definição do tipo de cada variável, precisa declarar o tipo de conteúdo que vai receber (Java, C, C++, TypeScript);
// Tipagem DINÂMICA -> O tipo de conteúdo é definido de acordo com o seu valor, não precisa ser informado na declaração (JavaScript, Python);
